/*
    Implements polynomial subtraction:

        POLY_SUB(res, poly1, len1, poly2, len2)

    Requires the following macros:

        * SET(x, y)
        * NEG(x, y)
        * VEC_SUB(v, v1, v2, len)
 */

long i, min = FLINT_MIN(len1, len2);

VEC_SUB(res, poly1, poly2, min);

if (poly1 != res)
    for (i = min; i < len1; i++)
        SET(res + i, poly1 + i);

for (i = min; i < len2; i++)
    NEG(res + i, poly2 + i);
